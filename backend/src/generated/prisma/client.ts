
/* !!! This is code generated by Prisma. Do not edit directly. !!! */
/* eslint-disable */
// biome-ignore-all lint: generated file
// @ts-nocheck 
/*
 * This file should be your main import to use Prisma. Through it you get access to all the models, enums, and input types.
 * If you're looking for something you can import in the client-side of your application, please refer to the `browser.ts` file instead.
 *
 * 🟢 You can import this file directly.
 */

import * as process from 'node:process'
import * as path from 'node:path'
import { fileURLToPath } from 'node:url'
globalThis['__dirname'] = path.dirname(fileURLToPath(import.meta.url))

import * as runtime from "@prisma/client/runtime/client"
import * as $Enums from "./enums"
import * as $Class from "./internal/class"
import * as Prisma from "./internal/prismaNamespace"

export * as $Enums from './enums'
export * from "./enums"
/**
 * ## Prisma Client
 * 
 * Type-safe database client for TypeScript
 * @example
 * ```
 * const prisma = new PrismaClient({
 *   adapter: new PrismaPg({ connectionString: process.env.DATABASE_URL })
 * })
 * // Fetch zero or more Challenges
 * const challenges = await prisma.challenge.findMany()
 * ```
 * 
 * Read more in our [docs](https://pris.ly/d/client).
 */
export const PrismaClient = $Class.getPrismaClientClass()
export type PrismaClient<LogOpts extends Prisma.LogLevel = never, OmitOpts extends Prisma.PrismaClientOptions["omit"] = Prisma.PrismaClientOptions["omit"], ExtArgs extends runtime.Types.Extensions.InternalArgs = runtime.Types.Extensions.DefaultArgs> = $Class.PrismaClient<LogOpts, OmitOpts, ExtArgs>
export { Prisma }

/**
 * Model Challenge
 * 
 */
export type Challenge = Prisma.ChallengeModel
/**
 * Model ChallengeSubmissionRequirementVersion
 * Versioned structural validation policy used by submission finalization.
 * `submissionRequirements` on Challenge remains organizer-facing guidance;
 * this row is the authoritative, immutable machine-enforced snapshot.
 */
export type ChallengeSubmissionRequirementVersion = Prisma.ChallengeSubmissionRequirementVersionModel
/**
 * Model ChallengeScheduleChange
 * A schedule or lifecycle change, recorded as domain history distinct from
 * (but linked to) the audit trail — this is queryable challenge-scoped
 * history, e.g. "show every deadline extension for this challenge."
 */
export type ChallengeScheduleChange = Prisma.ChallengeScheduleChangeModel
/**
 * Model ChallengeTrack
 * 
 */
export type ChallengeTrack = Prisma.ChallengeTrackModel
/**
 * Model ChallengePrize
 * Informational only. No payments, wallets, or payouts (master prompt 10.6).
 */
export type ChallengePrize = Prisma.ChallengePrizeModel
/**
 * Model ChallengeSponsor
 * 
 */
export type ChallengeSponsor = Prisma.ChallengeSponsorModel
/**
 * Model ChallengeTermsVersion
 * An immutable, versioned terms document a challenge requires participants
 * to accept (IP, confidentiality, publication permission, etc — master
 * prompt section 16). Never mutated in place; a material change creates a
 * new version.
 */
export type ChallengeTermsVersion = Prisma.ChallengeTermsVersionModel
/**
 * Model ConsentRecord
 * A user's acceptance of one specific, immutable terms version.
 */
export type ConsentRecord = Prisma.ConsentRecordModel
/**
 * Model Announcement
 * 
 */
export type Announcement = Prisma.AnnouncementModel
/**
 * Model Faq
 * 
 */
export type Faq = Prisma.FaqModel
/**
 * Model DataExport
 * 
 */
export type DataExport = Prisma.DataExportModel
/**
 * Model StoredObject
 * 
 */
export type StoredObject = Prisma.StoredObjectModel
/**
 * Model FileAsset
 * 
 */
export type FileAsset = Prisma.FileAssetModel
/**
 * Model OrganizationLimit
 * 
 */
export type OrganizationLimit = Prisma.OrganizationLimitModel
/**
 * Model FormDefinition
 * 
 */
export type FormDefinition = Prisma.FormDefinitionModel
/**
 * Model FormVersion
 * Immutable once published. A material change to a form creates a new
 * version rather than editing this row (master prompt section 11).
 */
export type FormVersion = Prisma.FormVersionModel
/**
 * Model FormResponse
 * Submitted responses are immutable. The participation owning workflow may
 * keep one mutable draft per user/form version until it is atomically
 * promoted to a submitted response and attached to a participation.
 */
export type FormResponse = Prisma.FormResponseModel
/**
 * Model ChallengeParticipation
 * 
 */
export type ChallengeParticipation = Prisma.ChallengeParticipationModel
/**
 * Model User
 * Global user account. Owned by Better Auth.
 * 
 * `emailVerified` gates every membership-granting path: invitation
 * acceptance, join-code redemption, organization application, and
 * challenge-staff invitation acceptance all require it.
 */
export type User = Prisma.UserModel
/**
 * Model Session
 * Durable session. Owned by Better Auth.
 * 
 * Sessions live in PostgreSQL, not Redis: a cache eviction or Redis restart
 * must never silently sign every user out. See ADR 0004.
 */
export type Session = Prisma.SessionModel
/**
 * Model Account
 * Credential or OAuth account linked to a user. Owned by Better Auth.
 * 
 * Provider tokens stored here are secrets: they are never serialized into any
 * API response, log line, or audit payload.
 */
export type Account = Prisma.AccountModel
/**
 * Model Verification
 * Email verification and password reset tokens. Owned by Better Auth.
 */
export type Verification = Prisma.VerificationModel
/**
 * Model TwoFactor
 * Two-factor enrolment. Owned by the Better Auth twoFactor plugin.
 * 
 * Mandatory for platform superadmins, optional for everyone else. The secret
 * and backup codes are credentials and are never returned by any API.
 */
export type TwoFactor = Prisma.TwoFactorModel
/**
 * Model PlatformRoleAssignment
 * Platform-wide role grant.
 * 
 * Modelled as its own table rather than a column on User so that grants carry
 * provenance — who granted it, when, and why — and so that revocation is a
 * state change with history rather than a silent overwrite.
 */
export type PlatformRoleAssignment = Prisma.PlatformRoleAssignmentModel
/**
 * Model UserProfile
 * The user-editable public-facing profile.
 * 
 * Separate from the Better Auth `user` row so that product profile fields can
 * evolve without touching a library-owned table, and so profile visibility is
 * a first-class column rather than an afterthought.
 */
export type UserProfile = Prisma.UserProfileModel
/**
 * Model Skill
 * Curated skill catalogue.
 * 
 * A normalised catalogue rather than a free-text string, so that matchmaking
 * and search can filter reliably. Organizers cannot create catalogue entries
 * directly; unrecognised values are held as controlled custom skills on
 * UserSkill instead.
 */
export type Skill = Prisma.SkillModel
/**
 * Model TechnologyTag
 * Curated technology-tag catalogue for the `/meta/technology-tags`
 * typeahead (master prompt section 34.1). Same shape and rationale as
 * `Skill` — a normalised catalogue, not a free-text scan of every
 * submission's own `technologyTags` array, which would require a
 * cross-tenant read of otherwise-private submission data for what is only
 * ever meant to be autocomplete suggestions.
 */
export type TechnologyTag = Prisma.TechnologyTagModel
/**
 * Model UserSkill
 * A skill claimed by a user.
 * 
 * Either references the catalogue (`skillId`) or carries a controlled custom
 * value (`customName`) — exactly one of the two, enforced by a check
 * constraint in migration SQL.
 */
export type UserSkill = Prisma.UserSkillModel
/**
 * Model AccountDeletionRequest
 * A user's request to delete their account.
 * 
 * Deletion is a workflow, not a DELETE: organization history, audit evidence,
 * and consent records must retain referential integrity, so the applied policy
 * pseudonymises rather than removes those (master prompt section 42).
 */
export type AccountDeletionRequest = Prisma.AccountDeletionRequestModel
/**
 * Model AuditEvent
 * Append-only business and security audit trail.
 * 
 * Written inside the same transaction as the change it records, so an audit
 * row exists if and only if the change committed. The runtime database role
 * is granted INSERT and SELECT only: it cannot UPDATE or DELETE these rows
 * (enforced in migration SQL, verified by integration tests).
 * 
 * `organizationId` is intentionally NOT a foreign key. Audit evidence must
 * outlive the organization it describes and must never be blocked or cascaded
 * by an organization-level operation.
 */
export type AuditEvent = Prisma.AuditEventModel
/**
 * Model OutboxEvent
 * Transactional outbox.
 * 
 * A business change and its required side effects are committed together: the
 * row here is the durable record that an effect is owed. A dispatcher later
 * publishes it to BullMQ using `id` as the job identifier, so a redelivery
 * cannot produce a duplicate job. Redis losing the queue can delay effects but
 * can never lose them (master prompt section 20).
 */
export type OutboxEvent = Prisma.OutboxEventModel
/**
 * Model IdempotencyRecord
 * Idempotency records for high-value retryable POST operations.
 * 
 * Keyed by actor + operation + client-supplied key. A replayed request returns
 * the stored response instead of performing the effect twice; a different body
 * under the same key is rejected rather than silently served (section 33).
 */
export type IdempotencyRecord = Prisma.IdempotencyRecordModel
/**
 * Model WebhookEvent
 * Inbound provider webhook receipts.
 * 
 * Persisted before any consequence is processed, and unique on the provider's
 * own event identifier so replayed deliveries are recognised and ignored
 * (sections 21 and 34.37).
 */
export type WebhookEvent = Prisma.WebhookEventModel
/**
 * Model Innovation
 * An innovation item either originates from a finalized submission
 * (`sourceSubmissionId`) or is created directly for continuous ideation.
 * `@@unique([sourceSubmissionId])` is what makes "one promotion per
 * submission by default" true at the database level, not just in service
 * logic — a second promote-to-innovation call for the same submission
 * fails on the constraint, not on a race-prone existence check.
 */
export type Innovation = Prisma.InnovationModel
/**
 * Model InnovationStageHistoryEntry
 * Append-only stage-gate history. A transition is always recorded here in
 * the same transaction that changes `Innovation.stage` — never inferred
 * after the fact from `updatedAt`.
 */
export type InnovationStageHistoryEntry = Prisma.InnovationStageHistoryEntryModel
/**
 * Model InnovationMilestone
 * 
 */
export type InnovationMilestone = Prisma.InnovationMilestoneModel
/**
 * Model InnovationEvidence
 * 
 */
export type InnovationEvidence = Prisma.InnovationEvidenceModel
/**
 * Model InnovationMetric
 * 
 */
export type InnovationMetric = Prisma.InnovationMetricModel
/**
 * Model InnovationMetricMeasurement
 * 
 */
export type InnovationMetricMeasurement = Prisma.InnovationMetricMeasurementModel
/**
 * Model Integration
 * 
 */
export type Integration = Prisma.IntegrationModel
/**
 * Model IntegrationDelivery
 * 
 */
export type IntegrationDelivery = Prisma.IntegrationDeliveryModel
/**
 * Model IntegrationDeliveryAttempt
 * 
 */
export type IntegrationDeliveryAttempt = Prisma.IntegrationDeliveryAttemptModel
/**
 * Model ChallengeStaffInvitation
 * Hashed single-use expiring token. No organization membership is granted
 * on acceptance (master prompt section 17.2).
 */
export type ChallengeStaffInvitation = Prisma.ChallengeStaffInvitationModel
/**
 * Model ChallengeStaffAssignment
 * The accepted, active (or removed) staff relationship itself.
 */
export type ChallengeStaffAssignment = Prisma.ChallengeStaffAssignmentModel
/**
 * Model Rubric
 * 
 */
export type Rubric = Prisma.RubricModel
/**
 * Model RubricVersion
 * Immutable once judging starts or any scorecard is submitted against it.
 * Criteria are relational rows so ranges, uniqueness, weights, and score
 * references are enforced by PostgreSQL rather than JSON validation alone.
 */
export type RubricVersion = Prisma.RubricVersionModel
/**
 * Model RubricCriterion
 * 
 */
export type RubricCriterion = Prisma.RubricCriterionModel
/**
 * Model JudgeAssignment
 * One judge-to-submission pairing. Reassignment creates a new row and marks
 * the old one REASSIGNED rather than mutating it, preserving history.
 */
export type JudgeAssignment = Prisma.JudgeAssignmentModel
/**
 * Model Scorecard
 * One scorecard per judge assignment. Relational criterion scores reference
 * the exact criterion snapshot. `totalScore` and
 * `maxPossibleScore` are the exact integer weighted sums computed server
 * side at submit time — never a client-supplied total.
 */
export type Scorecard = Prisma.ScorecardModel
/**
 * Model CriterionScore
 * 
 */
export type CriterionScore = Prisma.CriterionScoreModel
/**
 * Model SubmissionResult
 * One row per submission once results are finalized for a challenge.
 * Deliberately does not assume every challenge uses a strict numeric rank
 * (master prompt section 17.4): `rankLabel` is the display value
 * ("Winner", "Finalist", "Honorable Mention"), `rank` is optional.
 */
export type SubmissionResult = Prisma.SubmissionResultModel
/**
 * Model ResultSnapshot
 * Immutable organizer decision set. Publication changes only the lifecycle
 * state of this snapshot; the selected submission-version rows never change.
 */
export type ResultSnapshot = Prisma.ResultSnapshotModel
/**
 * Model MediaAsset
 * 
 */
export type MediaAsset = Prisma.MediaAssetModel
/**
 * Model ContentReport
 * 
 */
export type ContentReport = Prisma.ContentReportModel
/**
 * Model Notification
 * 
 */
export type Notification = Prisma.NotificationModel
/**
 * Model NotificationPreference
 * One row per user. `disabledCategories` lists categories the user has
 * turned off; categories in the non-disableable set are always delivered
 * regardless of what this array contains (enforced in the service layer,
 * not here — the column has no CHECK constraint tying it to the code's
 * notion of "essential", since that set is a product policy, not a schema
 * invariant).
 */
export type NotificationPreference = Prisma.NotificationPreferenceModel
/**
 * Model ReminderSchedule
 * 
 */
export type ReminderSchedule = Prisma.ReminderScheduleModel
/**
 * Model AnalyticsDailyRollup
 * 
 */
export type AnalyticsDailyRollup = Prisma.AnalyticsDailyRollupModel
/**
 * Model OrganizationApplication
 * A request to create an organization on the platform.
 * 
 * Exists before the organization does, so it is NOT tenant-scoped. Approval is
 * a single transaction that creates the organization, grants the applicant
 * ownership, writes the audit record, and emits the outbox event.
 */
export type OrganizationApplication = Prisma.OrganizationApplicationModel
/**
 * Model Organization
 * A tenant.
 */
export type Organization = Prisma.OrganizationModel
/**
 * Model OrganizationSettings
 * Per-organization configuration.
 * 
 * Separated from the profile so that changing a description and changing the
 * join policy are different operations with different permissions — the
 * generic profile PATCH must never be able to alter security-relevant state.
 */
export type OrganizationSettings = Prisma.OrganizationSettingsModel
/**
 * Model OrganizationMembership
 * A user's membership of an organization.
 */
export type OrganizationMembership = Prisma.OrganizationMembershipModel
/**
 * Model OrganizationInvitation
 * A direct, optionally email-bound invitation to join an organization.
 * 
 * Only the token HASH is stored. A database disclosure must not hand an
 * attacker a set of working invitations.
 */
export type OrganizationInvitation = Prisma.OrganizationInvitationModel
/**
 * Model OrganizationJoinCode
 * A shareable, high-entropy code that grants membership on redemption.
 * 
 * Only the hash is stored: there is deliberately no endpoint that reveals a
 * code after creation. The plaintext is returned exactly once, at creation.
 */
export type OrganizationJoinCode = Prisma.OrganizationJoinCodeModel
/**
 * Model OrganizationJoinRequest
 * A user's request to join an organization that uses REQUEST_TO_JOIN.
 */
export type OrganizationJoinRequest = Prisma.OrganizationJoinRequestModel
/**
 * Model Submission
 * The logical identity: one per team per challenge by default. Never holds
 * mutable content itself — every field a participant edits lives on a
 * `SubmissionVersion`, so judges can always evaluate an immutable snapshot.
 */
export type Submission = Prisma.SubmissionModel
/**
 * Model SubmissionVersion
 * An immutable, saved snapshot. A new version is created on every draft
 * save and on every finalize; nothing here is ever updated in place.
 */
export type SubmissionVersion = Prisma.SubmissionVersionModel
/**
 * Model SubmissionAsset
 * Bounded to four slots per version, enforced by the unique index on
 * (submission_version_id, slot) plus a check constraint on slot's range
 * (added in migration SQL) — the limit holds at the database level, not
 * only in application validation (master prompt section 15.5).
 */
export type SubmissionAsset = Prisma.SubmissionAssetModel
/**
 * Model SubmissionTechnology
 * 
 */
export type SubmissionTechnology = Prisma.SubmissionTechnologyModel
/**
 * Model SupportTicket
 * 
 */
export type SupportTicket = Prisma.SupportTicketModel
/**
 * Model SupportTicketComment
 * Visible to the ticket's own user and to platform staff.
 */
export type SupportTicketComment = Prisma.SupportTicketCommentModel
/**
 * Model SupportTicketInternalNote
 * Platform-staff-only. Never serialized to a participant-facing endpoint.
 */
export type SupportTicketInternalNote = Prisma.SupportTicketInternalNoteModel
/**
 * Model ChallengeTeam
 * 
 */
export type ChallengeTeam = Prisma.ChallengeTeamModel
/**
 * Model ChallengeTeamMember
 * One row per (challenge, user): a participant belongs to at most one team
 * per challenge at a time (master prompt section 14, "one active team per
 * challenge per participant by default").
 */
export type ChallengeTeamMember = Prisma.ChallengeTeamMemberModel
/**
 * Model TeamInvitation
 * Hashed single-use expiring token, accepted like an organization invitation.
 */
export type TeamInvitation = Prisma.TeamInvitationModel
/**
 * Model MatchmakingPost
 * A challenge-scoped matchmaking board post (master prompt section 14.1).
 */
export type MatchmakingPost = Prisma.MatchmakingPostModel
/**
 * Model MatchmakingInterest
 * Records an in-platform expression of interest in a matchmaking post.
 */
export type MatchmakingInterest = Prisma.MatchmakingInterestModel
/**
 * Model EmailSuppression
 * 
 */
export type EmailSuppression = Prisma.EmailSuppressionModel
/**
 * Model EmailDelivery
 * 
 */
export type EmailDelivery = Prisma.EmailDeliveryModel
/**
 * Model EmailDeliveryAttempt
 * Append-only evidence for every worker-level provider attempt.
 */
export type EmailDeliveryAttempt = Prisma.EmailDeliveryAttemptModel
